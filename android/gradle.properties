org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Compilation Kotlin incrementale desactivee : le cache incremental de Kotlin
# plante quand le cache Pub (C:) et le projet (D:) sont sur deux disques
# differents (calcul de chemin relatif impossible entre deux racines Windows).
# Contournement du bug, sans effet sur le resultat compile.
# Voir Journal de Developpement (etape 10, Lot B).
kotlin.incremental=false
